# 法院文书下载器 · 版本更新记录

## v2.4（2026-09-24）

**顶栏标识修正**（建哥反馈，界面细节）：

- 顶栏左上角的占位图形换成**真实软件图标**（深蓝底金天平，与 exe 文件图标/任务栏图标同源），圆角透明底自然融入顶栏；资源缺失时自动回退原占位样式
- 顶栏右侧文字由「全国法院统一送达平台」改为**软件实际名字「法院文书下载器 v2.4」**
- 其余界面与下载/更新逻辑零变化；UI 自检 26 项 + 既有回归全过

## v2.3（2026-09-24）

## v2.3（2026-09-24）

**界面全面美化（方案 A · 经典商务蓝）**——按参考稿重做整套 UI，功能与下载逻辑零改动：

- **主窗口重绘**：深蓝顶栏（金色 logo 块 + 版本副标）、圆角白卡片分区、扁平蓝色主按钮 / 白底描边次按钮（带悬停反馈），彻底告别 Windows 默认灰 3D 样式
- **选项区合并**：原三个 LabelFrame 合并为一张选项卡；单选组改为现代分段选择器（未勾选转 JPG 时目录模式自动置灰）
- **日志分级着色**：成功绿 / 警告琥珀 / 失败红 / 标题蓝，下载状态一眼可辨
- **进度条重绘**：圆角细条，取消时转琥珀色提示
- **DPI 感知**：150% / 200% 缩放屏文字不再发虚，子窗口（使用说明 / 更新弹窗 / 下载进度框）尺寸随 DPI 自适应
- **使用说明窗口**配套美化；更新弹窗字体统一为雅黑 UI、主色统一商务蓝
- 新增界面控件库 ui_kit.py（随包发货）；下载、更新、配置保存等核心逻辑无任何变化
- 自检：UI 状态机 23 项 + 既有回归（33 + 14 + GUI 冒烟）全部通过

## v2.2（2026-09-22）

## v2.2（2026-09-22）

### 更新下载改为「GitHub 加速镜像优先，自有服务器兜底」

- **下载前并发测速择优**：对 gh-proxy.com / ghfast.top / ghproxy.net 三个加速镜像做
  256KB 测速，按实测速度从快到慢尝试；GitHub 原站与自有服务器直链保留在候选里兜底。
- **排序改为「组优先」**：加速镜像 → GitHub 原站 → 自有服务器，本站在任何情况下都排最后。
  修掉了旧排序的一个缺陷：自有服务器偶尔测速偏高时会插到镜像前面，违背「镜像优先」。
- **探测失败的源不丢弃**，只排到本组末尾，最后仍会试一次 —— 任何镜像全挂都还能更新。
- **检查更新同样优先走加速镜像**（`update.json` 经镜像读取），自有服务器
  `/updates/court.json` 退为兜底。

**为什么改**
- 实测裸网直连 GitHub 只有 3.7 KB/s（基本等于不可用），而公共加速镜像可达 439 KB/s
- 镜像之间也能差近 10 倍，所以不能死按固定顺序试，必须测速择优
- 自有服务器受阿里云 ECS 固定带宽约束（实测封顶约 445 KB/s），已无速度优势，改为兜底

## v2.1（2026-09-22）

### 自动更新改为「国内服务器优先，GitHub 兜底」
- 检查更新与下载新版本**优先访问自有下载站**：<http://47.116.64.26:8888/>
  —— 起因：用户反馈从 GitHub 下载又慢又容易超时
- **双下载源**：更新元数据里同时给出「服务器直链」与「GitHub 直链」，
  下载时逐个尝试，某个源不可用自动切换，单个源抖动不影响更新
- 下载完成后按元数据里的 SHA256 校验完整性，杜绝半截包被当成新版装上
- 官网同步改造：页面优先从自有服务器取版本信息（国内秒开），
  两个下载按钮直接指向服务器直链；服务器不可用时自动回退 GitHub

### 说明
- 界面与其余功能无变化；更新弹窗、进度、取消逻辑保持 v2.0 的形态

## v2.0（2026-09-16）

### 界面调整
- **移除窗口左下角的「检查更新」按钮**：更新入口已统一收敛到菜单栏「更新 → 检查更新」，界面更清爽。
- **更新弹窗三按钮样式统一**：重做为扁平设计——「立即更新」蓝色实底白字（主操作），「本次忽略」「以后不再提醒」浅灰底深字（次操作），三者字号、内边距一致；去掉原先渲染参差的 emoji 图标。下载进度框的「取消更新」按钮同步为同款样式。

### 说明
- 下载、更新等核心逻辑无任何变化。

## v1.9（2026-09-16）

### 新增
- **项目仓库地址写入软件内**：「帮助 → 使用说明」第一行与「帮助 → 关于」弹窗均置顶显示仓库地址
  （`https://github.com/jianRY/dianzisongda-xiazaiqi`），新版本和历史版本下载随时可查。

### 说明
- 本版为内容补充版本，核心下载与更新逻辑与 v1.8 完全一致。

## v1.8（2026-09-13）

一次全面的「隐形 bug」排查（逐行审计 + 33 项回归测试），并新增**可安装到系统的安装版**。

### 修复
- **「以后不再提醒」会被悄悄重置**：勾了这个选项后，只要再改动任意一个设置（保存路径、转 JPG…），配置里的 `auto_update` 就被写回 `true`，下次启动又开始弹更新提示。根因是保存配置的函数把 `auto_update` 默认成 `true`，覆盖了更新模块写入的 `false`。
- **非 PDF 文书一律被判失败**：下载后强制校验 `%PDF-` 文件头，但法院也可能发 jpg、docx 等格式 —— 这类文书会被判为「文件头异常」，重试 3 次仍失败。现在只在 `.pdf` 上做该校验；任何类型仍会拦截服务端返回的 XML/HTML 错误页。
- **「跳过已存在的同名文书」从未真正生效**：案件文件夹名带启动时间戳，每次运行都是新文件夹，文件永远不存在，跳过分支形同虚设。现在识别到案号时会**复用同一案件上次的文件夹**，已下过的文书真正跳过（省时省流量）；未识别到案号时仍按时间戳新建，避免同名法院不同链接被合并。
- **极端情况下新版本接管文件名失败**：旧版备份文件名是固定的 `xxx_旧版.exe`，一旦上次更新留下同名残留，改名的「目标已存在」错误会让接管彻底失败 —— 用户会一直启动到旧版本。现改为带时间戳的备份名并带冲突兜底。
- **自动更新可能误下安装包**：发布 Release 会同时上传「绿色版」和「安装版」两个 exe，旧逻辑取第一个 `.exe`，有把安装器当成新版程序下载的风险。现在明确跳过名字含 setup / installer / 安装 的资产。
- **文件名边界问题**：Windows 保留设备名（CON、PRN、NUL、COM1…）会导致保存失败，已加前缀规避；超长文书名按 100 字符截断，避免突破路径长度上限。
- **扩展名字段异常值**：`c_wjgs` 若返回 `application/pdf` 这类值，旧代码会拼出非法文件名；现已清洗为合法扩展名。
- **窗口关闭后写日志抛异常**：下载线程在窗口销毁后仍可能记日志，`after()` 会抛 TclError 打断收尾流程，现已吞掉该异常。

### 新增
- **安装版（Setup）**：除原有绿色单文件版外，每次发布同时产出一个安装包。
  - 开始菜单 + 桌面快捷方式、系统「已安装的应用」里可正常卸载。
  - **仅安装到当前用户**（`%LOCALAPPDATA%\Programs`），全程不需要管理员权限；这一点是刻意的 —— 装到 `C:\Program Files` 这类只读目录会导致内置自动更新无法写入而失效。
  - 安装向导为简体中文，界面配色与程序图标一致。
  - 两种版本功能完全一致，绿色版仍然照常提供，按喜好选一个即可。

### 优化
- 复用案件文件夹时若本次新勾选了「转 JPG」，会自动补转之前未转换的 PDF，不再让人以为转换丢了。
- 更新检查按钮/菜单在只有安装包的 Release 上会明确提示「未找到可下载的更新文件」，而不是下错东西。

## v1.7（2026-09-13）

本次专门重做自动更新，修掉「点了更新、程序也关了、版本却没变」这个顽疾。

### 修复
- **自动更新整体失效（根因）**：旧实现会生成一个 `updater.bat`，脚本里用「旧程序文件是否还存在」当作「进程是否已退出」的判断条件 —— 但这个文件永远存在（它正是要被替换的那一个），条件永不成立，脚本只能一路空等到 60 秒超时，最后什么都不做。这就是「弹窗说要重启替换、程序确实关了、版本却没变」的真正原因。
- **替换机制重做，不再依赖任何外部脚本**：新版 exe 直接放进程序所在文件夹并以中间名启动；新版本跑起来之后，删掉旧版本文件、再把程序文件名接管回来。全程无需 bat，也没有任何「等待进程退出」的轮询。
- **新版本启动会静默失败（Windows 隐藏限制）**：实测发现，在「当前进程自己的映像路径」上启动新进程时，引导进程能起来、但真实程序起不来，表现为「程序关了却没有新窗口」。因此新版一律先用中间名（`xxx_更新中.exe`）启动，起来后再改名接管。已连续两轮更新实测通过。

### 说明
- 更新后的**程序文件名保持不变**（跟随你原来的命名），桌面与任务栏快捷方式继续有效。
- 程序请放在**有写入权限**的目录（如桌面、文档）；放在只读目录中无法自动更新。
- 更新过程中可能短暂出现 `xxx_更新中.exe`，属正常现象；新版接管后会自动清理，万一残留，下次启动也会清掉。
- ⚠️ **v1.6 及更早版本无法自动升级到本版** —— 它们的更新机制本身是坏的，只能空等超时。请手动下载本版 exe 覆盖旧文件即可，配置与使用习惯不受影响。

## v1.6（2026-09-13）

本次为程序标识更新：给软件配上专属图标（此前一直沿用 Python 默认图标）。

### 新增
- **应用图标**：深蓝底配金色天平，含 16 / 24 / 32 / 48 / 64 / 128 / 256 共 7 种尺寸，小尺寸下依然清晰。
  - exe 文件图标（资源管理器、桌面快捷方式）
  - 程序窗口标题栏图标
  - 「使用说明」窗口图标
- **Windows 任务栏身份标识（AppUserModelID）**：此前任务栏只显示 Python 默认图标，现在固定为本程序图标。

### 说明
- 图标文件 `assets/app.ico` 随打包一并分发；若文件缺失程序会自动跳过设置，不影响任何功能。

## v1.5（2026-09-13）

本次为代码审计修复版：修掉 1 个长期存在的老 Bug + 5 项健壮性问题 + 3 项体验优化。

### 修复
- **案号识别正则失效（老 Bug，从 v1.0 起就存在）**：原正则要求汉字后紧跟案件类型字，但真实案号形如「(2025)苏0505民初7780号」，汉字与「民」之间夹着法院代码数字，导致**从未匹配成功**——所有案件文件夹其实一直没带案号。已重写正则，支持 `(2025)苏0505民初7780号`、`（2025）苏05民终1234号`、`(2024)京01执异88号` 等各类标准案号。
- **多案件批量时案号错配**：原先按链接前后 140 字符窗口抓案号，多条短信混贴时会把相邻案件的案号安错。改为「全文扫描案号 → 按位置就近配对」，链接在前、案号在后也能正确配对。
- **下载完整性无校验**：原先只检查文件头是不是 `%PDF` 与大小非零，网络中断时可能留下「文件头正常但内容被截断」的坏 PDF。现在比对 `Content-Length`，不一致即重试。
- **更新后程序不退出导致替换失败**：点「立即更新」下载完成后，替换脚本会空等主程序退出而主程序并未退出，最终更新不生效。现在安装前回调 + 主动退出（含 `os._exit` 兜底），确保替换真正完成。
- **替换脚本无限等待**：`updater.bat` 原先无条件死等旧进程释放文件。现加 60 秒超时保护，超时则不删旧版（保证程序仍可用），并提示新版所在位置供手动替换。

### 优化
- **跳过已存在的同名文书**（新增开关，默认开启）：同一案件重复下载时不再重复拉取，省时省流量；需强制重下时关掉开关即可。
- **下载中关闭窗口加确认**：原先直接点 X 会硬杀线程、留下半成品文件；现在会询问并让任务收尾清理。
- **更新弹窗居中修正**：原先启动检查时主窗尚未完成布局，弹窗会跑到屏幕角落；现将窗口尺寸未就绪时兜底为屏幕居中。
- 移除无效的 `root.iconbitmap()` 空调用；`save_config` 调用点统一收敛，避免漏参。

## v1.4（2026-09-05）

在 v1.3 基础上重做自动更新流程，并抽出通用更新模块 autoupdate.py 供其他软件复用。

### 新增
- **更新弹窗（三选项）**：启动自动检查更新，发现新版弹窗显示**本次更新内容**（取自 GitHub Release 说明，自动清理 markdown 符号），可选：
  - ✅ 立即更新
  - ⏭ 本次忽略（下次启动继续检查）
  - 🚫 以后不再提醒（写入配置 `auto_update=false`，启动不再自动检查；菜单「更新 → 检查更新」仍可手动检查）
- **下载进度对话框**：确认更新后显示进度条、已下载/总大小、百分比与实时速度（EMA 平滑），**可随时取消**（取消即清理临时文件，不影响当前版本）。
- **通用模块 `autoupdate.py`**：零第三方依赖，三行代码接入，含 Windows 自替换重启（updater.bat）。本工具的更新流程与它完全一致，可直接复制到其他带更新功能的软件。

### 变更
- 配置文件 `%APPDATA%\法院文书下载器\config.json` 新增 `auto_update` 字段（缺省 true）。
- 旧版「是/否」更新弹窗与无进度下载逻辑移除，统一由 autoupdate 模块承担。

## v1.3（2026-09-04）

在 v1.2 基础上新增多线程并发下载与若干 Bug 修复、界面优化。

### 新增
- **多线程并发下载**：同一案件的文书清单一次性拿到后，用 `ThreadPoolExecutor`（默认 4 线程）并发下载，明显缩短多份文书的总耗时；案件之间仍串行（避免 OSS 签名跨案件过期）。
- **取消下载按钮**：下载中「⬇ 开始下载」按钮自动变为「⏸ 取消下载」，点击后弹出二次确认，未完成的任务会尽快停止；已下载的文件保留。
- **进度百分比**：进度条标签新增百分比显示，从「进度：3 / 10 份」升级为「进度：3 / 10 份 (30%)」。
- **日志时间戳**：每条日志自动加上 `[HH:MM:SS]` 前缀，便于回溯问题。

### 修复
- **占位符未清空直接点开始**：若文本框仍是占位符（示例链接）或为空，点击开始下载不再用 `...` 当参数去调真实接口，而是直接提示「请先粘贴」。
- **保存路径预校验**：开始下载前会尝试在所选路径建一个临时测试文件，不可写时直接弹错并建议改路径，避免下载到一半才因路径问题整体失败。
- **`os.startfile` 跨平台**：抽出 `_open_in_explorer` 统一处理，非 Windows 平台（macOS / Linux）改用 `open` / `xdg-open`，不再 AttributeError。

### 优化
- **PyMuPDF 线程安全**：并发下载完成后转 JPG 时使用全局 `threading.Lock` 串行化所有 fitz 调用，避免 PyMuPDF 跨线程崩溃。
- **重名竞态保护**：并发下载前先用 `used_names` 集合确定每份文书的本地路径，避免并发场景下多个线程同时检测 `os.path.exists` 撞名。
- **取消信号下传到 download_file**：`download_file` 新增 `cancel_check` 回调，在每次读 buffer 前检查取消标志，被取消时立即抛 `_Cancelled` 退出，删半成品文件。

## v1.2（2026-09-03）

在 v1.1 基础上新增菜单栏与自动更新能力。

### 新增
- **顶部菜单栏**：
  - 「更新」菜单 → 「检查更新」（手动触发，等价于底部按钮）。
  - 「帮助」菜单 → 「使用说明」（弹出滚动窗口，含使用步骤、自动更新说明、常见问题 FAQ）与「关于」。
- **自动检查更新**：程序启动时静默检查 GitHub 最新 Release，发现更高版本弹窗提示，确认后自动下载新 exe、退出并由 updater 脚本替换旧文件、重启。

### 说明
- 仓库需为公开（或 Release 公开）才能免密自动更新；私有仓库会提示需设为公开。

## v1.1（2026-08-09）

在 v1.0 基础上新增功能。

### 新增
- **「🗑 清空」按钮**：位于「📋 粘贴」按钮旁，一键清空文本框内容并恢复置灰占位符。

## v1.0（2026-08-01）

首个正式版本，功能完整、可双击即用（单文件 exe）。

### 定位
自动从「全国法院统一送达平台」电子送达短信 / 链接中，下载该案件下全部文书 PDF 到本地，支持批量、转图与防合并命名。

### 核心功能
1. **链接自动识别**：粘贴整段送达短信或仅链接，自动提取 `zxfw.court.gov.cn` 的送达链接（支持 `qdbh/sdbh/sdsin` 参数）。
2. **免登录取清单**：调用文书列表接口 `POST .../getWsListBySdbhNew`，拿到该案件全部文书（OSS 签名链接有效期极短，运行时现取现下；签名绑定 HTTP 方法，只用 GET）。
3. **批量下载**：一次粘贴多个链接，自动去重并依次下载，互不干扰。
4. **图形界面**：粘贴文本窗口 + 开始下载按钮 + 实时日志 + 进度条（已下载 / 总份数）。
5. **保存路径可选 & 记忆**：默认 `桌面\文书`（不存在自动新建）；可手填、点「选择路径…」、「恢复默认」；上次选择写入 `%APPDATA%\法院文书下载器\config.json` 并自动回填。
6. **完成后打开文件夹（三档）**：不打开 / 打开保存根目录 / 打开每个案件文件夹。
7. **粘贴增强**：「📋 粘贴」按钮、文本框右键菜单（粘贴/复制/剪切/全选）、置灰占位符（点击或粘贴自动清空，无需先删举例）。
8. **仅下载 PDF**：输出只包含 PDF 文件，无 txt / json 残留；重名自动加序号（如 `文书(1).pdf`）。
9. **自动转 JPG（可勾选）**：将下载的 PDF 转为高质量 JPG（长边 2000px，短边按比例，quality=95）；多页自动加页码（`文件名_页码.jpg`）。
   - 模式①：所有图片统一放在案件下 `图片/` 文件夹。
   - 模式②：按 PDF 文件名在 `图片/<文件名>/` 下各自建子文件夹。
10. **防合并命名**：每个案件文件夹命名为 `法院名_案号_启动时间戳`，时间戳为任务启动的 14 位年月日时分秒纯数字（`YYYYMMDDHHMMSS`），避免同名法院 / 同案号的不同链接合并到同一文件夹。

### 界面选项一览
- ① 粘贴法院送达短信或链接（支持批量）
- ② 保存路径（可编辑 / 选择 / 恢复默认）
- 下载完成后打开文件夹：不打开 / 打开保存根目录 / 打开每个案件文件夹
- 下载后转换为图片 (JPG)：勾选 + 模式①所有图一夹 / 模式②按文件名分夹
- ⬇ 开始下载、进度条、下载日志、打开保存文件夹、清空日志

### 技术说明
- 语言：Python 3（Tkinter GUI，标准库实现下载逻辑）。
- 转图依赖：PyMuPDF (fitz) 1.28.0。
- 打包：PyInstaller `--onefile --windowed --noupx --hidden-import=fitz`，产物 `法院文书下载器.exe`（约 31.5 MB，单文件、双击即用）。
- 配置持久化：`%APPDATA%\法院文书下载器\config.json`（记住路径、打开方式、转图开关与目录模式）。
- 平台：全国法院统一送达平台 `zxfw.court.gov.cn`。

### 已知限制 / 兜底
- OSS 签名链接不可复用，必须每次运行重新取链接后立刻下载。
- 若接口改版导致取清单失败，兜底方案是用浏览器（如 Playwright 无头）打开链接拦截接口响应或点页面下载按钮。
